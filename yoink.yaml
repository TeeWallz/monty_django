# generated by yoink 0.20.9; edit freely.
# https://oddur.github.io/yoink/docs/reference/config

hosts:
  - { address: vm-monty.nix03.jenga.xyz, user: yoink }

secrets:
  provider: age
  recipients:
    - age1sj0dvy9cyqq62paxw0pkhaxchteduqkedu7w6lt5e7rcnsx0rsrqeuveac

proxy:
  enabled: true

services:
  - name: monty-django
    image: ghcr.io/nrhtr/monty_django
    tag: latest
    # frontend/vanity domain
    domain: vm-monty.nix03.jenga.xyz
    tls: "off"   # 'host' Caddy terminates TLS before it hands off to guest :/
    run:
      port: 8000
      healthcheck_path: /health
      options:
        user: "1500:1500"
      volumes:
        - /var/data/monty/media:/app/media
        - /var/data/monty/db:/app/db
    secrets:
      - SECRET_KEY
    env:
      # add vanity domain here! (comma-separated)
      ALLOWED_HOSTS: vm-monty.nix03.jenga.xyz
      CSRF_TRUSTED_ORIGINS: https://vm-monty.nix03.jenga.xyz
      DEBUG: "false"
      DB_DIR: /app/db
